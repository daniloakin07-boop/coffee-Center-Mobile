// Endereço centralizado do backend para que as telas não precisem repetir a URL.
// Em produção, este valor aponta para a API publicada no Render.
export const API_URL = 'https://coffee-center-mobile.onrender.com';